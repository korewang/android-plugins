<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.korewang.shuishui.MatrixActivity" >
    <TextView 
        android:id="@+id/scroll"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:layout_margin="10dp"
        android:padding="10dp"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusableInTouchMode="true"
        android:focusable="true"
        android:text="如果一根火柴不能算作一个 1的话如果一根火柴不能算作一个 1的话如果一根火柴不能算作一个 1的话如果一根火柴不能算作一个 1的话11"/>
    <ImageView 
        android:id="@+id/imageview"
        android:layout_height="150dp"
        android:layout_width="200dp"
        android:src="@drawable/ic_launcher"/>
    <TextView
        android:id="@+id/textscale"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
         android:layout_marginTop="10dp"
        android:text="宽度：200高度150" />
	<SeekBar 
	    android:id="@+id/seekbarscale"
	    android:layout_width="200dp"
	    android:layout_height="wrap_content"
	    android:max="240"
	    android:progress="120"
	    android:layout_marginTop="10dp"/>
	 <TextView
        android:id="@+id/textdegress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
         android:layout_marginTop="10dp"
        android:text="0度" />

	<SeekBar
	    android:id="@+id/seekbardegress"
	    android:layout_width="200dp"
	    android:layout_height="wrap_content"
	    android:layout_below="@+id/imageview"
	    android:layout_marginTop="10dp"
	    android:max="360" />

	<ListView
	    android:id="@+id/myList"
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content" >
	</ListView>

</LinearLayout>
